// Moved to components/notifications/NotificationBell.tsx (P2.4 Task 2).
// Public props unchanged — this re-export keeps existing imports working;
// delete once all consumers import the new path (P2.4 Task 9).
export { default } from "@/components/notifications/NotificationBell";
export type { NotificationItem } from "@/components/notifications/NotificationBell";
